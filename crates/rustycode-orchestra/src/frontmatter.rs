//! Frontmatter re-exported from rustycode_protocol for backward compatibility.
//!
//! This module is kept for backward compatibility.
//! Use `rustycode_protocol::frontmatter` directly for new code.

pub use rustycode_protocol::frontmatter::{
    parse_frontmatter_map, split_frontmatter, FrontmatterMap, FrontmatterValue,
};
